/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: allowedDevOrigins is for Next.js 15+, for 14.x we don't need it
  // as the warning is non-breaking. If you upgrade, you might need to re-add this.
};

export default nextConfig;
