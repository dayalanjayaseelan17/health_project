/**
 * @name Hackathon Development Rules
 * @description These rules are intentionally open for a hackathon environment.
 * They allow all read and write operations to facilitate rapid development and
 * prevent permission-related blockers.
 *
 * WARNING: These rules are NOT secure for a production application.
 * Before deploying to a live environment, you should implement strict,
 * user-based security rules to protect user data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes to any document in the database.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
